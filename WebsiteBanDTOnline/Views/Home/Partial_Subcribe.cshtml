@model WebsiteBanDTOnline.Models.EF.Subcribe

<div class="newsletter">
    <div class="container">
        <div class="row">
            <div class="col-lg-6">
                <div class="newsletter_text d-flex flex-column justify-content-center align-items-lg-start align-items-md-center text-center">
                    <h4>Đăng ký nhận thư điện tử</h4>
                    <p>Đăng ký ngay để nhận thông tin ưu đãi mới nhất</p>
                </div>
            </div>
            <div class="col-lg-6">
                @using (Ajax.BeginForm("Subscribe", "Home", new AjaxOptions
                {
                    OnBegin = "OnBeginSub",
                    OnSuccess = "OnSuccessSub",
                    OnFailure = "OnFailureSub"
                }))
                {
                    @Html.AntiForgeryToken()
                    <script>
                        function OnBeginSub() { }
                        function OnSuccess(rs) {
                            if (rs.Success) {

                            }
                        }
                        function OnFailureSub(rs) { }
                    </script>
                    <div class="newsletter_form d-flex flex-md-row flex-column flex-xs-column align-items-center justify-content-lg-end justify-content-center">
                        @Html.TextBoxFor(x => x.Email, new { @placeholder = "Email của bạn", @type = " email", @required = "required", @id="newsletter_email" })
                        <button id="newsletter_submit" type="submit" class="newsletter_submit_btn trans_300" value="Submit">Đăng ký</button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

